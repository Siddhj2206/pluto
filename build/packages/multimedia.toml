# multimedia — full codec set (negativo17). Consumed by 25-multimedia.sh:
# [multimedia_overrides] -> mesa/libva swaps from fedora-multimedia + versionlock
# [fedora]               -> one install --enablerepo=fedora-multimedia
# [vendor_assert]        -> must be negativo17.org vendor (assert_vendor)
#
# Repo kept enabled for runtime codec updates (bluefin disables and lints;
# pluto intentionally keeps enabled). Repofile source (25-multimedia.sh):
# https://negativo17.org/repos/fedora-multimedia.repo (priority pinned to 90
# in-script). Install outputs are vendor-asserted negativo17.org below, so a
# repofile swap fails the build instead of slipping in silently.
# Verified 2026-08-28, updated 2026-08-29 f44:
# - ffmpeg/libavcodec/libfdk-aac are negativo17 only (Fedora has *-free)
# - gstreamer: -bad-free split -> unsplit bad (neg17 obsoletes), libav -> plugin-libav
# - intel-vpl-gpu-rt: in bluefin's 12 overrides but absent on f44 negativo
#   (repoquery f44 shows no such package — skip; --skip-unavailable would hide)
# - libpostproc: removed in ffmpeg 8 (F44, 8.1.2) — no RPM, only Provides: from
#   libavutil; dropped (otherwise rpm -q assert fails like mesa-va-drivers)

[multimedia_overrides]
packages = [
    "intel-mediasdk",
    "intel-gmmlib",
    "libheif",
    "libva",
    "libva-intel-media-driver",
    "mesa-dri-drivers",
    "mesa-filesystem",
    "mesa-libEGL",
    "mesa-libGL",
    "mesa-libgbm",
    "mesa-vulkan-drivers",
]

[fedora]
packages = [
    "ffmpeg",
    "ffmpeg-libs",
    "libavcodec",
    "libavdevice",
    "libavfilter",
    "libavformat",
    "libavutil",
    "libswresample",
    "libswscale",
    "libfdk-aac",
    "gstreamer1-plugins-base",
    "gstreamer1-plugins-good",
    "gstreamer1-plugins-bad",
    "gstreamer1-plugins-ugly",
    "gstreamer1-plugin-libav",
    "lame",
    "lame-libs",
    "libjxl",
    "ffmpegthumbnailer",
    "pipewire-libs-extra", # negativo17 rebuild (bluefin vendor assert)
]

[vendor_assert]
packages = [
    "ffmpeg",
    "libavcodec",
    "libfdk-aac",
    "mesa-dri-drivers",
    "mesa-vulkan-drivers",
    "libheif",
    "libva",
    "pipewire-libs-extra",
    "x264-libs",
    "x265-libs",
]

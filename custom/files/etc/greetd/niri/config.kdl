// Greeter-only niri baseline. dms-greeter manages this instance (greetd
// config.toml passes this path via -C) — do NOT add outputs/keybinds here;
// the file exists so the path resolves, content is dms-greeter's.
